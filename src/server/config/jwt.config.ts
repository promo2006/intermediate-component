// Constraseña con la que se encriptan las sesiones y se generan los tokens
export const JWT_SECRET: string = '';
// Tiempo de expiracion de las sesiones (en segundos)
export const JWT_EXPIRATION_TIME: number = 7 * 24 * 60 * 60;
